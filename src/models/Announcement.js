export class Announcement {
  constructor(id, type, creationTime, value, title) {
    this.id = id
    this.type = type
    this.creationTime = creationTime
    this.value = value
    this.title = title
  }
}
